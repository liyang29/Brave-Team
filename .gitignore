# Godot 4 导入缓存（自动生成，不入库）
.godot/

# Claude Code worktrees（临时工作副本，不入库）；但 .claude/skills 要入库
.claude/worktrees/

# 本机 MCP 服务（lsp-bridge 含 48M node_modules + 本机绝对路径，不入库）
.claude/mcp/

# 临时/输出
*.tmp
*_out.txt
